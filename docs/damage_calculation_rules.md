# 데미지 계산식 기준

이 문서는 Rune Nexus의 데미지 계산식을 추가하거나 변경할 때 따라야 하는 기준이다.
새 젬, 특성, 적 저항, 포탑 피해 보정, 특정 대상 피해 보정을 만들 때는 이 문서를 먼저 확인한다.

## 용어

- 저항: 적이 받는 피해를 줄이거나 늘리는 방어 계층 수치다. `10% 저항`은 피해 배율 `90%`를 의미한다.
- 취약: 저항을 낮추는 음수 방향 보정이다. `물리 저항 -20%`처럼 표현한다.
- 배율: 별도의 피해 계층에서 곱해지는 공격 쪽 보정이다. `물리 피해 배율 +30%`는 `1.3x` 계층이다.
- 증가: 같은 계층 안에서 더해지는 보정에 사용한다.
- 감소: 같은 계층 안에서 빼는 보정에 사용한다.
- 증폭: 합산이 끝난 계층 결과에 곱해지는 보정에 사용한다.
- 감폭: 합산이 끝난 계층 결과에 `1 - 감폭률`을 곱하는 보정에 사용한다.

`증가·감소`는 같은 계층끼리 먼저 합산하고, `증폭·감폭`은 합산 결과에
각각 별도로 곱한다.

```text
증가 20% + 증가 10% - 감소 5% = 같은 계층에서 25% 증가
증폭 20% = 앞선 계산 결과 * 1.20
감폭 20% = 앞선 계산 결과 * 0.80
```

확률, 저항, 피해 배율처럼 비율 수치 자체를 직접 바꾸는 효과는 `%p`로
표시한다. 사용자 문구에서 곱연산 감소를 `-10%`처럼 축약하지 않고
`10% 감폭`으로 표현한다.

## 기본 원칙

같은 계층 안에서는 합연산을 먼저 끝낸다.
서로 다른 계층끼리는 그 결과를 곱한다.

```text
최종 피해 = 기본 피해 * 저항 계층 * 피해 배율 계층 * 최종 증폭 계층
```

계층 내부에 합연산 보정과 곱연산 보정이 같이 있다면, 합연산 보정을 먼저 계산한 뒤 곱연산을 적용한다.

치명타는 저항이 아니라 공격 쪽 피해 배율 계층으로 본다. 예를 들어 치명타 피해 배율 `1.5x`는 기본 피해와 포탑/젬/특성 피해 보정이 반영된 공격 피해에 곱하고, 이후 적 저항 계층과 함께 최종 피해를 만든다.

치명타 피해 영구 강화는 최종 배율을 다시 곱하지 않는다. 포탑의 기본 치명타 추가 피해율에 `%p` 단위로 합산한다.

```text
치명타 피해 배율 = 1 + 기본 치명타 추가 피해율 + 영구 강화 추가 피해율
```

예를 들어 기본 치명타 피해 배율 `1.5x` 포탑은 기본 추가 피해율이 `50%p`다. 치명타 피해 강화 15레벨은 `+15%p`를 더하므로 최종 치명타 피해 배율은 `1.65x`다. 기본 치명타 피해 배율 `2.0x`인 저격 포탑은 같은 강화에서 `2.15x`가 된다.

## 저항 계층

저항은 배율 데이터가 아니라 저항률 데이터로 저장한다.

```text
저항 0% = 피해 배율 100%
저항 30% = 피해 배율 70%
저항 -30% = 피해 배율 130%
저항 -110% = 피해 배율 210%
```

산식:

```text
피해 배율 = 1 - 저항률
```

저항률은 양수 방향으로만 상한을 둔다. 아무리 높은 저항이 쌓여도 실제 적용 저항은 최대 `90%`다.
음수 저항, 즉 취약은 이 상한으로 막지 않는다.

```text
저항 150% => 적용 저항 90% => 피해 배율 10%
저항 -110% => 피해 배율 210%
```

## 저항 데이터 표현

적 데이터에는 `0.9` 같은 피해 배율을 쓰지 않는다.
저항률로 표현한다.

```text
기존 표현: 물리 피해 배율 0.9
새 표현: 물리 저항 0.10
```

예시:

```text
보스 물리 저항 10%
파쇄 충격: 적 물리 저항 -20%

최종 물리 저항 = 10% - 20% = -10%
물리 저항 계층 피해 배율 = 110%
```

## 계층 분리

물리 저항과 물리 취약은 같은 계층이다.
따라서 먼저 합산한다.

```text
물리 저항 10% + 물리 저항 -20% = 물리 저항 -10%
```

반면 `보스 피해 배율 +10%` 같은 효과는 저항 계층이 아니다.
저항 계층 결과와 별도로 곱한다.

```text
물리 저항 계층 110% * 보스 피해 배율 110% = 최종 121%
```

## 최종 증폭 계층

`균열 낙인`처럼 적이 받는 전체 피해를 늘리는 효과는 저항/취약이 아니라 최종 증폭 계층으로 둔다.
최종 증폭은 저항 계층과 공격 쪽 피해 배율 계층이 계산된 뒤, 보호막/방어구/체력 계위에 들어가기 전 입력 피해에 곱한다.

```text
균열 낙인 일반 적 25% => 최종 증폭 1.25x
균열 낙인 보스 12.5% => 최종 증폭 1.125x
```

## 태그 저항

계열 저항과 태그 저항은 서로 다른 저항 계층으로 본다.
각 계층 안에서는 저항률을 합산하고, 계층 결과끼리는 곱한다.

```text
물리 저항 20%, 중화기 저항 -20%

물리 계층 = 1 - 0.20 = 80%
중화기 계층 = 1 - (-0.20) = 120%
최종 저항 계층 = 80% * 120% = 96%
```

공격이 여러 태그를 가진다면 각 태그 저항 계층을 차례로 곱한다.

## 포탑 스탯 보정

포탑 스탯도 같은 원칙을 따른다.

폭발 반경 예시:

```text
(기본 반경 + 중화기 젬 증가 + 파편 장전 증가 + 확장 폭심 증가) * 폭발 젬 증폭
```

합연산 증가가 모두 끝난 뒤 곱연산 증폭을 적용한다.

코어 공격 패시브도 이 순서를 따른다.

```text
포탑 화력 = 기존 포탑 피해 계산 결과 * 화력 동기화 증폭
코어 스킬 위력 = 기본 효과 * 출력 증폭 * 임계 출력 증폭 * 초월 출력 증폭
```

`출력 증폭`은 `%p` 가산이나 증가 계층이 아니라 코어 스킬의 기본 피해 또는
기본 증폭률에 곱하는 별도 증폭이다. `임계 출력`과 `초월 출력`도 각각
별도 증폭으로 곱한다. 따라서 최대 랭크 기준 일반 발동은
`1.25 * 1.25 = 1.5625x`,
3회차 발동은 `1.25 * 1.40 * 1.25 = 2.1875x`다.

## 코어 효율 패시브

`젬 스펙트럼`은 모든 배치 포탑에 실제 장착된 서로 다른 젬 종류를 센다.
서로 다른 젬이 3종 이상일 때만 발동하며 최대 6종까지만 계산한다.
포탑 모듈의 장착 젬 효과 증가를 먼저 계산한 뒤 패시브 증폭을 별도로 곱한다.

```text
수치형 젬 효과 배율
  = (1 + 포탑 모듈 젬 효과 증가율)
    * (1 + 젬 스펙트럼 랭크별 비율 * 적용 젬 종류 수)
```

예를 들어 포탑 모듈의 젬 효과 증가가 20%이고, 최고 랭크 젬 스펙트럼이
서로 다른 장착 젬 6종으로 발동하면 `1.20 * 1.18 = 1.416x`다.
이 배율은 피해, 공격 속도, 사거리, 치명타 확률, 조준 속도, 지속피해,
폭발 반경 등 기존 수치형 젬 보정에 적용한다. 장갑 관통 여부와 연쇄 횟수
같은 규칙형 효과도 발동 종류 수에는 포함하지만 규칙 자체는 비율 증폭하지 않는다.

효율 패시브의 비용 감폭은 기존 비용 보정을 대체하지 않고 별도 계층으로
곱한다. 포탑 모듈 건설 비용 감소는 기존 20% 하한을 유지하고, 그 결과에
`절약 설계`와 활성화된 `통합 전선`을 차례로 곱한다.

```text
건설 비용
  = round(
      모듈 감소와 20% 하한을 적용한 비용
      * (1 - 절약 설계 감폭률)
      * 통합 전선 감폭 배율
    )

레벨업 비용
  = round(
      기본 비용
      * 기존 모듈 감소 배율
      * (1 - 다양성 감응 감폭률 * 중첩 수)
      * 통합 전선 감폭 배율
    )

링크 확장 비용
  = round(
      기본 비용
      * 기존 모듈·연구 감소 배율
      * (1 - 연결 최적화 감폭률)
      * 통합 전선 감폭 배율
    )
```

`다양성 감응` 중첩은 `서로 다른 배치 포탑 종류 수 - 1`이며 0~4로 제한한다.
`통합 전선`은 서로 다른 포탑 4종 이상이 현재 배치된 동안 `0.85x`로
적용한다. 네 번째 종류를 건설하는 비용에는 적용되지 않고, 배치 완료 후의
건설·레벨업·링크 확장부터 적용한다. 특성 젬 파편 비용과 라운드 클리어
골드에는 적용하지 않는다. 모든 골드 비용의 최솟값은 1이다.

`특성 공학`은 1·2차 특성의 기본 젬 파편 비용을 별도 계층에서 감폭하고
한 번 반올림한다.
최고 랭크에서는 기본 비용 12·24가 각각 9·18이 되며 최솟값은 1이다.

라운드 클리어 골드는 기본 보상과 기존 영구·런 보너스를 먼저 더한 뒤
`보급 회수` 증폭을 곱하고 한 번 반올림한다.

```text
라운드 클리어 골드
  = round(
      (기본 클리어 보상 + 영구 보너스 + 런 보너스)
      * (1 + 보급 회수 증폭률)
    )
```

## 넥서스 방위 패시브

넥서스 피해는 적의 기본 넥서스 피해에 `충격 분산`과 `위협 약화`를
서로 다른 감폭 계층으로 곱해 계산한다.

```text
잃은 내구도 비율
  = 1 - (현재 체력 + 현재 보호막 + 현재 방어구)
        / (최대 체력 + 최대 보호막 + 최대 방어구)

최종 넥서스 피해
  = 적 기본 넥서스 피해
    * (1 - 충격 분산 감폭률)
    * (1 - 위협 약화 최대 감폭률 * 잃은 내구도 비율)
```

두 감폭 효과를 최고 랭크로 적용했을 때의 이론상 최종 배율은
`0.85 * 0.75 = 0.6375x`다. 즉, 총 감폭률은 `36.25%`다.

넥서스의 현재·최대 체력과 실제 피해·회복은 `double`로 계산하고
중간 반올림하지 않는다. HUD와 넥서스 피해·회복 피드백만 소수점
최대 한 자리로 표시한다. 회복은 최대 체력을 넘지 않으며, 치명 피해는
라운드 종료 회복 전에 실패로 확정한다.

`최종 방위선`으로 무효화한 피해는 실제 손실이 아니므로 `손상 복원`의
라운드 손실량에 포함하지 않고 `비상 충전`도 발동하지 않는다. 보스는
`최종 방위선`을 적용하거나 라운드 사용 기회를 소모하지 않는다.

`비상 충전`은 현재 코어 스킬의 전체 재사용 대기시간에 랭크별 회복률을
곱한 만큼 남은 시간을 즉시 차감한다. 스킬이 이미 준비됐거나 발동
중이라 회복할 재사용 대기시간이 없으면 라운드 사용 기회를 소모하지 않는다.

## 방어 계위 설계 초안

방어 계위는 아직 구현 전 설계 기준이다.
1차 구현에서는 속성 약점표를 전면 규칙으로 두지 않고, 내구도 계위가 요구하는 공격 방식 차이를 먼저 검증한다.

피해 적용 순서:

```text
보호막 -> 방어구 -> 체력 -> 사망 판정
```

기존 저항, 피해 배율, 최종 증폭 계산이 끝난 값을 계위 입력 피해로 본다.
이후 보호막, 방어구, 체력 계위가 순서대로 피해를 흡수한다.

### 계위 역할

- 보호막: 별도 내구도를 가진 재생 외피다. 체력보다 먼저 피해를 받으며, 순간 피해보다 지속 화력과 집중 공격으로 돌파하는 계위로 둔다.
- 방어구: 별도 내구도를 가진 장갑이다. 체력보다 먼저 피해를 받으며, 남아 있는 동안 작은 타격을 감쇄한다. 속사와 약한 지속피해보다 한 방 피해가 큰 포탑이 유리한 계위로 둔다.
- 체력: 최종 생명력이다. 보호막과 방어구를 통과한 남은 피해를 받는다.

1차 구현에서는 `화염은 체력에 강함`, `전기는 보호막에 강함`, `부식은 방어구에 강함` 같은 속성 약점표를 넣지 않는다.
화상, 전기 과부하, 부식 약화, 냉각 둔화 같은 효과는 젬, 특성, 특정 포탑의 개성으로 확장한다.

### 데이터 모델

내구도 계위는 적 정의와 전투 중 상태를 분리한다.

적 정의에는 최대값과 규칙값을 둔다.

```text
maxShield
shieldRegenRate
maxArmor
maxHp
```

전투 중 적 상태에는 현재값과 파괴 여부를 둔다.

```text
shield
shieldBroken
armor
hp
```

`maxShield`, `maxArmor`가 0이면 해당 계위가 없는 적으로 본다.
방어구는 보호막처럼 재생하지 않는다.
보호막은 `shieldBroken`이 `true`가 되면 더 이상 재생하지 않는다.

### 방어구 감쇄

방어구가 남아 있는 동안에는 최대 방어구 기준 압박값으로 입력 피해를 감쇄한 뒤, 그 결과 피해를 방어구 내구도에 먼저 적용한다.
압박값은 해당 적의 전투 진입 시점 최대 방어구 기준으로 계산하며, 현재 방어구가 줄어도 바뀌지 않는다.
감쇄 상수는 전역 고정값으로 두고, 적별 차이는 최대 방어구량으로만 조절한다.

```text
장갑 압박값 = sqrt(최대 방어구) * 3
방어구 피해율 = 12% + 88% * 입력 피해 / (입력 피해 + 장갑 압박값)
방어구 피해 = 입력 피해 * 방어구 피해율
체력 관통 피해 = max(0, 방어구 피해 - 현재 방어구)
```

방어구 피해가 현재 방어구보다 작거나 같으면 체력은 피해를 받지 않는다.
방어구 피해가 현재 방어구보다 크면 방어구를 0으로 만들고, 남은 방어구 피해만 체력으로 넘어간다.
체력으로 넘어간 피해에는 방어구 감쇄를 다시 적용하지 않는다.
장갑 관통 젬은 방어구 감쇄 산식을 무시하고 입력 피해를 그대로 방어구 피해로 취급한다. 방어구 계위를 우회하지 않으며, 방어구가 남아 있으면 체력에 직접 피해를 주지 않는다.

공통 상수:

```text
기본 흠집 피해율: 12%
피해 반응 구간: 88%
장갑 압박 계수: 3
```

기본 흠집 피해율은 속사, 지속피해, 보조 피해가 완전히 무력화되는 것을 막기 위한 하한이다.
피해 반응 구간은 입력 피해가 클수록 방어구 피해율이 원 피해에 가까워지는 구간이다.
압박값을 최대 방어구의 제곱근으로 계산해 방어구가 낮은 적과 높은 적 모두에서 피해 차이가 유의미하게 남도록 한다.

예시:

```text
적 최대 체력 1000
최대 방어구 120
현재 방어구 120
장갑 압박값 sqrt(120) * 3 = 32.86

입력 피해 7   -> 방어구 피해 1.92, 체력 피해 0
입력 피해 25  -> 방어구 피해 12.51, 체력 피해 0
입력 피해 40  -> 방어구 피해 24.12, 체력 피해 0
입력 피해 150 -> 방어구 피해 126.28, 체력 피해 6.28
```

### 보호막 재생

보호막은 매초 최대 보호막의 일정 비율만큼 재생한다.
1차 구현에서는 피격 시 재생 지연을 두지 않는다.
다만 보호막이 0이 되면 해당 보호막은 파괴된 것으로 보고 더 이상 재생하지 않는다.
플레이어가 `보호막 재생량보다 높은 지속 DPS로 한 번 돌파해야 한다`고 이해할 수 있게 단순하게 유지한다.

이 규칙은 보호막이 전투를 계속 지연시키는 장기 재생 체력이 아니라, 돌파 전까지만 압박을 주는 추가 보호 계위로 동작하게 하기 위한 기준이다.

보호막 피해는 별도 감쇄 없이 현재 보호막에 먼저 적용한다.
입력 피해가 현재 보호막보다 크면 보호막을 0으로 만들고 남은 피해가 방어구로 넘어간다.
보호막이 0이 된 순간 `shieldBroken`을 `true`로 기록한다.

보호막 초과 피해는 사라지지 않는다.
예를 들어 현재 보호막이 100이고 입력 피해가 120이면 보호막은 0이 되고, 남은 20 피해가 방어구 계위로 넘어간다.
방어구가 남아 있다면 그 20 피해는 방어구 감쇄 산식을 거친 뒤 방어구 내구도에 적용된다.
방어구가 없거나 이미 0이면 남은 피해는 체력으로 넘어간다.

예시:

```text
최대 보호막 200
초당 재생률 5%
초당 재생량 10

DPS 8  -> 보호막 돌파 불가
DPS 20 -> 천천히 돌파, 보호막 파괴 뒤 재생 없음
DPS 100 -> 빠르게 돌파, 보호막 파괴 뒤 재생 없음
```

### 저장과 복구

진행 저장에는 내구도 계위의 현재 상태를 포함한다.

저장 대상:

- 현재 보호막
- 보호막 파괴 여부
- 현재 방어구
- 현재 체력

저장하지 않는 항목:

- 보호막 최대값
- 방어구 최대값
- 보호막 재생률
- 방어구 공통 감쇄 산식 버전

최대값과 규칙값은 적 정의에서 다시 읽는다.
저장 데이터가 오래되어 보호막/방어구 필드가 없으면 0으로 복구해 기존 저장과 호환한다.
복구 시 현재 보호막과 현재 방어구는 각 최대값 범위로 clamp한다.

### UI 표시

하단 적 미리보기와 실제 적 표시 모두 같은 계위 정보를 보여준다.

- 보호막: 위쪽 줄에 파란색 바를 표시한다.
- 체력과 방어구: 아래쪽 줄에 `체력 | 방어구` 형태로 함께 표시한다.
- 아래쪽 줄의 비율 기준은 `최대 체력 + 최대 방어구`다.
- 방어구가 남아 있으면 체력 구간 오른쪽에 회색/금속색 방어구 구간을 표시한다.
- 방어구가 0이 되면 아래쪽 줄은 기존 체력 바처럼 체력만 1줄로 표시한다.
- 보호막이 없거나 0이면 보호막 줄을 표시하지 않는다.
- 적 미리보기에는 체력, 방어구, 보호막 수치를 함께 보여준다.

바 표기는 계위 순서와 체력 대비 방어구 비율을 시각적으로 보여주기 위한 표시다.
실제 피해 적용 순서는 항상 `보호막 -> 방어구 -> 체력`을 따른다.

### 1차 콘텐츠 적용

방어구는 공통 내구도 보정이 아니라 장갑병 전용 정체성으로 둔다.
일반, 빠름, 탱커, 보스는 기본 방어구가 없고 각자의 속도/체력/보스 역할로 구분한다.

```text
장갑병
- 보호막 없음
- 방어구 많음
- 체력 보통
- 최대 방어구 기준 공통 감쇄 산식
- 속사 포탑으로 잡히지만 비효율적
- 대포/저격처럼 한 방 피해가 큰 포탑이 유리
```

장갑병의 시작 수치는 다음을 기준으로 한다.

```text
체력: 40
방어구: 28
방어구 비율: 체력의 70%
속도: 28
보상: 7골드
```

장갑병은 챕터 3 진입 이후 본격적으로 등장한다.
챕터 1은 생명력 기반 적만 사용하고, 챕터 2는 보호막병을 먼저 소개해 내구 계위 학습 순서를 분리한다.

보호막 몹, 속성 상호작용, 상태이상 확장은 방어구 몹의 체감 검증 뒤에 추가한다.

## 구현 위치

- 적 저항 데이터: `lib/data/definitions/game_enemy_data.dart`
- 저항률 계산 규칙: `lib/domain/enemy/enemy_resistance_profile.dart`
- 전투 피해 계산: `lib/game/rune_nexus_game.dart`
- 포탑 스탯 계산: `lib/game/components/turret_component.dart`
