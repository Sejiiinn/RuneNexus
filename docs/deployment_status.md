# 배포 상태와 세션 간 인계

배포 요청을 처리할 때 이 문서와 [운영 배포 절차](self_hosted_api_deployment.md)를 먼저 확인한다.
이 문서는 마지막으로 검증한 이력이며 실시간 운영 상태가 아니다. 배포 직전 실제 DB 버전,
실행 중인 API 이미지, 공개 웹 커밋, 최신 APK release를 다시 확인한다.

## 마지막 확인된 운영 상태

2026-09-09 배포 완료 기준:

- 배포 커밋: `ddde6736fbfe482562c67a44b1af291d22bad089`.
- API: `rune-nexus-api:ddde673`, 운영 DB migration 010 적용. HTTPS health/live·health/ready 정상.
- 웹 workflow: `34244434333` 성공. 공개 웹 HTTP 200, main.dart.js의 배포 커밋과 운영 API 주소 확인.
- Android workflow: `34244435998` 성공. 최신 공개 release는 `apk-9`, versionCode 9, versionName 0.1.8.
  최신 update.json의 버전·APK 크기·SHA-256이 공개 release asset과 일치한다.
  `apk-6`, `apk-7`, `apk-8`에서의 차등 업데이트 생성·서명 동일성·Kotlin 복원 검증을 통과했다.
  공개 차등 업데이트 파일의 SHA-256도 manifest와 일치한다.
- 배포 전 DB 백업의 pg_restore 목록 검사와 별도 PostgreSQL 18 인스턴스 전체 복원 완료.
  복원 DB에서 009→010 마이그레이션과 인증·저장·리더보드 통합 테스트를 통과했다.
  백업과 비밀 설정은 Git에 넣지 않는다.

## 이번 배포 변경

- 보상 젬을 전장에서 직접 장착하고, 만석 포탑에서는 기존 젬을 보관하며 교체한다.
- 모바일 장착 화면, 중앙 교체 모달, 젬 주효과와 공통 공격 규칙 안내를 개선했다.
- 글로벌 폭발·연쇄·효과 범위 규칙, 메인 로비 화면 고정, 진행 중 클리어 기록의 리더보드 반영을 포함한다.
- 젬 UI 변경에는 저장 형식 변경이나 추가 migration이 없다. 리더보드 변경에 필요한 010은
  새 API 시작 전에 적용했다.

## 검증 범위와 복구 참고

- Flutter 전체 테스트 663개 통과, 기존 skip 10개. 서버 단위 테스트와 복원 DB 통합 테스트 통과.
- 모바일 장착·교체·보관 UI는 실제 Flutter 게임 렌더와 터치 테스트로 검증했다.
- 운영 계정으로 실제 기기 Google 로그인·진행 저장을 수행한 검증은 이번 자동 배포에 포함하지 않았다.
- API는 배포 커밋에서 추출한 서버 소스로 빌드했다. 이전 이미지 `rune-nexus-api:8ccb32b`를 유지한다.
- 010은 기존 기록을 보존한다. 저장 출처 기록이 생긴 뒤에는 010 down이 거부되므로 기록을 삭제해
  down을 강제하지 않는다. API 롤백과 DB down은 별도로 판단하고 확장된 스키마 유지를 우선한다.

## 미배포: 인증 응답 장기 복구와 저장 재시도

- 서버: 최신 갱신 응답을 다음 갱신 성공·세션 종료까지 보존하며, 만료 access가 포함된
  exact 응답도 복구한다. 기존 client는 child refresh로 새 access를 발급받는다.
- 클라이언트: 일시 인증 오류의 저장 재시도 및 재로그인 후 인증 차단 Outbox 복구.
- 추가 migration·환경값 변경 없음. 기존 고정 receipt 암호화 키를 유지한다.
- 모든 API 인스턴스와 정리 worker를 함께 교체한다. 구버전 worker는 기존 유한
  expires_at의 최신 암호문을 삭제할 수 있으므로 혼용하지 않는다.
- 이미 구버전에서 암호문이 삭제됐거나 세션이 폐기된 경우에는 한 번 재로그인이 필요하다.
  서버 변경만으로 이미 폐기된 세션을 되살리지는 않는다.
- 운영 API·웹·APK에는 아직 적용하지 않았다.

## 기록 유지 규칙

- 배포가 필요한 코드 변경에는 필요한 migration·환경 설정·선행 조건을 이 문서에 함께 기록한다.
- 배포 후 검증된 커밋, DB 버전, workflow/release, 검증 결과를 갱신하고 미배포 항목을 정리한다.
- 실패·부분 완료는 구성 요소별로 기록한다. 과거 대화나 로컬 작업 기록만으로 완료를 판단하지 않는다.
- 비밀번호, 토큰, 암호화 키, DB 백업 내용은 이 문서나 커밋에 넣지 않는다.
