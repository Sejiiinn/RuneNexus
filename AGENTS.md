## 기본 원칙

- 한글로 설명하세요.
- 임시 코드나 단순히 동작만 맞춘 코드를 넣지 마세요. 현재 프로젝트 구조와 맞물리는 형태로 구현하세요.
- 목적 달성에 필요한 최소한의 수정만 진행하세요.
- 목표 달성에 직접 필요한 국소 구조 정리는 가능하지만, API 계약 변경·데이터 손실 가능성·대규모 책임 경계 변경·외부 의존성 추가가 필요하면 먼저 사용자에게 확인하세요.
- 이득이 크지 않은 단순 wrapping에 가까운 helper 메서드는 만들지 마세요.
- 이해가 어려울 수 있는 코드 구문에는 짧은 명사형·구문형 주석을 추가하세요.

## GSD 작업 방식

- 모호한 요구사항은 임의로 해석하지 않습니다.
- 코드에서 확인 가능한 사실은 사용자에게 묻지 말고 직접 확인합니다.
- 사용자가 "고쳐줘", "반영해줘", "해결해줘"처럼 구현 의도를 명확히 표현한 경우는 코드 작성 지시로 봅니다.
- 구현 전에 작업 범위와 비범위를 정리합니다.
- 구현 전 계획에는 다음을 포함합니다:
  - 수정할 파일
  - 수정하지 않을 파일
  - 예상 변경 요약
  - 완료 기준
  - 검증 방법
- 사용자 의사 결정이 필요한 핵심 분기가 있으면 구현 전에 질문합니다.
- 사용자가 구현 중 구조나 책임 경계를 여러 차례 바꾸려 할 경우, 현재 단계의 목표·비목표·커밋 단위를 기준으로 변경 필요성을 먼저 검토합니다.

## 변경 제한

- 기존 UI 문구, 레이아웃, public API, 저장 로직은 명시 요청 없이는 변경하지 않습니다.
- 사용자가 요청하지 않은 리디자인, 구조 개편, view-model 분리는 하지 않습니다.
- 지금 단계의 핵심 목표와 직접 관련 없는 구조 변경은 보류를 제안합니다.
- 이미 합의된 책임 경계가 있다면 그 경계를 유지하는 쪽으로 중심을 잡습니다.
- 데미지 계산식, 저항, 취약, 피해 배율, 포탑 스탯 보정을 추가하거나 변경할 때는 반드시 `docs/damage_calculation_rules.md`를 먼저 확인하고 그 계층 규칙을 따르세요.

## Commit Guidelines

- 커밋 메시지는 한글로 작성하고, 아래 항목을 참고하여 앞에 prefix를 붙여주세요.
1. 기능 추가 - feat:
2. 버그 수정 - fix:
3. 리팩토링 - refactor:
4. 기타 수정 - chore:
- Flutter 실행 뒤 생성 파일이나 줄바꿈만 바뀐 파일이 섞일 수 있으므로 커밋 전에는 의도한 변경만 스테이징하세요.
- 플랫폼 generated plugin 파일은 요청 범위가 아니면 커밋에 포함하지 마세요.

## 참조 가이드

- Windows에서 Flutter/Dart 실행, 포맷, analyze/test/build/run, 53000 포트 확인, 인앱 브라우저 검증이 필요한 경우에는 먼저 `.agents/windows_flutter_guide.md`를 읽고 그 우선순위를 따르세요.
- 인앱 테스트 화면을 열거나 UI·게임플레이·렌더링 변경을 눈으로 확인해야 하는 경우에는 `.agents/windows_flutter_guide.md`를 먼저 확인한 뒤 `.agents/in_app_test_guide.md`를 따르세요.
- 샌드박스 밖 실행 승인이 필요한 명령은 `.agents/sandbox_command_guide.md`를 따르세요. 단, Windows Flutter 관련 명령이면 `.agents/windows_flutter_guide.md`의 실행 방식과 우선순위를 먼저 적용하세요.
