# RuneNexus 작업 기준

- 승인된 작업은 필요한 구현·결함 수정·검증까지 완료한다. 현재 구조에 맞춰 요청한 결과를 완성한다. 필요한 국소 구조 정리는 가능하며, 저장 호환성·API 계약·합의한 책임 경계를 보존한다. 요청에 포함되지 않은 데이터 손실, 호환성 파괴, 대규모 구조·제품 변경은 해당 변경 전에 확인한다.
- 문서가 필요하면 [작업별 지도](docs/README.md)에서 관련 기준과 필요한 절을 고른다. 제안·역사 기록을 현행 요구로 사용하지 않는다. 문서 작성·갱신·병합은 [문서 유지 지침](docs/documentation_guide.md)을 따른다. 상태는 지정된 원본 한 곳에서 관리하고 다른 문서에는 링크한다.
- 성능 개선·대량 표시·CPU/GPU 작업 분배를 다룰 때는 [최적화 지침](docs/performance_optimization_guidelines.md)을 따른다. 현재 병목과 기존 최적화를 확인하고, CPU 작업 감소·GPU 비용·전체 프레임 성능을 구분해 판단한다.
- 최종 엔진 방향은 Godot 단일 앱이다. 기존 전투·디자인·저장·API 계약을 보존하며 이관한다. 현재 실행 경로와 단계별 완료·미완료 상태는 [이관 로드맵](docs/godot_unified_app_roadmap.md), 전투 책임은 [현행 경계](docs/godot_combat_migration_boundaries.md)를 따른다. 과거 Flame 전투·2D fallback 기록을 현행 책임으로 사용하지 않는다.

- 버그 수정은 증상·근거를 확인한 뒤 필요한 최소 변경을 적용하고 같은 조건으로 재검증한다. 재현이 어려우면 로그·코드 등 확보한 근거와 검증 한계를 명시한다. 요청 밖 UI 재설계를 섞지 않는다.

## UI·시각 에셋

- UI·레이아웃·시각 에셋 변경에는 [DESIGNS.md](DESIGNS.md)의 공통 방향과 해당 화면 기준을 적용한다. 승인 시안의 핵심 형태·색·재질을 보존하며, 기법 변경 승인을 외형 변경 승인으로 해석하지 않는다. 중복 시각 검수 생략 조건과 최종 인게임 판정은 같은 문서의 [검증과 완료 기준](DESIGNS.md#검증과-완료-기준--완벽한-복제보다-요청-충족)을 따른다. 품질과 인게임 재현률을 토큰 절감보다 우선한다.
- 3D 원본·애니메이션·프레임 렌더는 Blender MCP를 우선하고 기존 Python/bpy 제작·수정·내보내기 경로를 재사용한다. 그 경로에서 필요한 2D 보정·알파·크롭·패킹은 GIMP MCP를 사용한다. 3D 원본이나 프레임별 제어가 필요 없는 일반 2D 생성·편집은 ImageGen을 사용하며, 사용자가 지정한 도구와 편집 가능한 원본을 우선한다.
- 열린 원본·미저장 작업을 보존하고 기존 도구 인스턴스를 재사용한다. 연결 복구·산출물 점검은 [에셋 작업 절차](.agents/asset_workflow_guide.md)를 따른다.
- 3D 에셋의 입체 형태·깊이·재질·조명 반응·움직임은 Blender 원본에서 구현한다. GIMP로 그린 명암·윤곽·발광을 평면에 입혀 필요한 입체 표현을 대신하지 않는다. 표면 텍스처 제작과 렌더 후처리는 가능하지만, 회전·조명 변화·애니메이션에서도 필요한 입체성이 유지되는지 확인한다. 빌보드·스프라이트·플립북으로 대체하는 방식은 해당 에셋에 합의된 표현 방식일 때 사용하며, 합의된 표현 방식·외형·편집 가능한 원본을 바꾸는 미합의 변경은 적용 전에 확인한다. 이를 보존하는 내부 구현 선택은 별도 재승인 없이 처리한다.
- 일반 게임 시안·에셋 요청은 위 제작 도구로 처리한다. Creative Production은 해당 앱이나 보드 작업이 요청에 포함될 때 선택한다. Blender 시안만 요청한 작업의 완료 범위에 게임 이식을 추가하지 않는다.
- 제작 원본·중간 산출물은 `design/`, 게임용 최종 이미지는 `assets/images/`에 두고 기존 로딩 경로에 연결한다. 산출물 점검은 [에셋 작업 절차](.agents/asset_workflow_guide.md)를 따른다.
- 이미지 에셋을 임시 Godot 도형으로 대체하지 않는다. 게임 상태에 반응하는 가벼운 보조 표현만 인게임 렌더링으로 구현한다.

## 전투·배포

- 데미지 계산식·저항·취약·피해 배율·포탑 스탯 보정 변경은 [데미지 계층 규칙](docs/damage_calculation_rules.md)을 따른다.
- 엔진·네이티브 의존성·대용량 에셋 변경 시 포함 대상과 중복을 점검한다. 아래 조건에 따라 APK를 만들 때 [APK 용량 점검](docs/android_apk_distribution.md#용량-점검)을 수행하며, 공개 배포 전 이전 공개 APK와 실제 크기·증가 원인을 비교한다. 용량 점검만을 위해 중간 APK를 만들지 않는다.
- Android 배포 옵션·환경값은 `.github/workflows/deploy-apk.yml`이 기준이다. 웹 배포 경로는 폐기했다.

## 독립 검증 적용

독립 검증의 대상·예외·담당 구성은 공통 지침의 「하위 에이전트와 문맥 관리 → 구현과 독립 검증」을 따른다. 검증 실행 범위는 아래 표와 해당 계약에 맞춰 선택하며, 독립 검증을 이유로 전체 회귀·게임플레이·APK 빌드를 일괄 추가하지 않는다. 시각 작업의 합격 기준과 근거 재사용 조건은 [DESIGNS.md](DESIGNS.md#검증과-완료-기준--완벽한-복제보다-요청-충족)를 따른다.

## 변경 대상별 검증

변경한 책임에 맞는 검사만 선택한다. 실제 화면의 완료 기준은 [DESIGNS.md](DESIGNS.md#검증과-완료-기준--완벽한-복제보다-요청-충족)를 따른다. 테스트 저장·생성 디렉터리는 사용자 플레이와 동시 작업에서 격리한다([실행·격리 절차](.agents/in_app_test_guide.md#테스트-저장과-생성-디렉터리-격리)).

| 변경 대상 | 검증 진입점 |
| --- | --- |
| Godot 전투·런·저장 | [네이티브 회귀 실행기](scripts/run_godot_native_regressions.py), [저장 검사](godot/app/README.md#검사). 관련 사례를 선택하고 Godot 실행 파일이 없으면 통과로 취급하지 않음 |
| Godot UI·렌더·앱 수명주기 | [인앱 검증](.agents/in_app_test_guide.md), [편집 프로젝트](.agents/godot_mcp_guide.md) |
| Android 호스트·브리지 | 관련 자동 검사와 변경된 플랫폼 경계의 Android 검증 |
| Go API·DB | [서버 Makefile](server/Makefile)의 test·vet 및 변경 계약의 관련 통합 검사. DB 환경은 [로컬 DB 가이드](docs/local_postgresql_setup.md) |
| 문서만 변경 | [문서 완료 확인](docs/documentation_guide.md#완료-확인) |

## Godot 검증과 APK 빌드

- 기본 경로: 일반 UI·메뉴·게임 로직·카메라·효과 위치는 Godot 직접 실행과 관련 자동 검사로 먼저 확인한다. 수정은 이 경로에서 마무리한 뒤 필요한 기기 검증을 묶어 수행한다. 도구 장애는 [복구·대체 경로](.agents/in_app_test_guide.md#도구-복구와-대체-경로)로 처리하며 APK 반복 빌드 사유로 삼지 않는다.
- APK 허용 조건: 사용자 요청, Android에서만 확인 가능한 항목, 최종 배포 검증 중 하나가 있을 때만 빌드·설치한다. Android 항목은 폰트/SP·밀도·안전 영역, 터치·시스템 뒤로가기·수명주기, 네이티브 연동, 모바일 GPU·실기기 성능 등이며 데스크톱에서 검증 가능한 변경은 제외한다.
- 빌드 사유: 실행 전에 데스크톱으로 확인할 수 없는 구체적 항목 또는 사용자 요청·배포 목적을 한 문장으로 밝힌다. 사유가 없으면 빌드하지 않으며, 이 설명을 별도 승인 질문으로 만들지 않는다.
- 결과 구분: 자동 검사·Godot 화면·Android 기기 결과를 구분한다. skip·미검증을 통과로 처리하지 않는다. 필요한 최종 Android 본게임·계정·배포 검증은 유지하되, 이를 중간 수정마다 APK를 요구하는 것으로 해석하지 않는다.

## 커밋

커밋 메시지는 한글로 작성하고 `feat:`, `fix:`, `refactor:`, `chore:`를 사용한다. 의도한 변경만 스테이징하며, 요청 밖의 플랫폼 generated plugin 파일·자동 줄바꿈 변경은 제외한다. 커밋·푸시는 요청이나 기존 승인 범위에 있을 때 수행한다.
