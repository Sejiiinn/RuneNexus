Use case: background-extraction. Asset type: production fantasy game gem icon. Edit target: supplied concept board. Extract ONLY the LARGE CENTER pale blue SCOPE gem (조준경): near-circular rough gemstone lens with nested concentric glass rings, icy white blue faceted rim, cyan pinpoint core and dark craggy stone underside. Keep its exact approved silhouette, orientation, cool palette, concentric structure, faceted weathered material and details; do not redesign. Delete all other gems, small duplicate icons, Korean labels, textured backdrop and floor shadow. Output one isolated object centered on a genuinely transparent RGBA PNG background, 1024x1024. Scale the full silhouette to fit within 88% of canvas on its longest dimension, keeping all edges intact and clear transparent padding on all sides. No text, no frame, no sheet, no checkerboard painted into pixels, no opaque background, no ground plane.

Alpha correction attempt:
Use case: background-extraction. Correct the supplied gem cutout: remove the gray checkerboard background entirely and export with REAL ALPHA TRANSPARENCY. Preserve the gemstone exactly unchanged including outer silhouette, all facets, rings, color, internal glow and orientation. Transparent pixels must have alpha zero. The current checkerboard is erroneously baked into RGB; it must disappear, not be repainted. One centered isolated gem with transparent margin on every side. No background color, no checkerboard, no shadow, no text. PNG with genuine alpha channel.

Mode: built-in image_gen.
