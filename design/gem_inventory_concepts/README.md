# 보유 젬 인벤토리 시안

2026-09-08. 내장 ImageGen으로 제작한 배치 비교용 이미지입니다. 후속 승인으로 A안을 게임에 반영했습니다.

- A `concept-a-tray.png`: 한 줄 슬롯 인벤토리. 세로 공간을 줄이고 가로로 넘기며 탐색.
- B `concept-b-grid.png`: 4열 × 2행 격자. 여러 젬을 동시에 비교, 선택 효과는 아래에 표시.
- C `concept-c-split.png`: 왼쪽 3열 × 2행 인벤토리와 오른쪽 고정 상세. 목록과 설명을 함께 표시.

각 이미지의 생성·수정 프롬프트는 같은 이름의 `concept-*-prompt.txt`에 기록했습니다.
참고 화면은 `../gem_sockets/hud-link-assets-preview.png`입니다. 일부 주변 문구·배경·장식은 이미지 생성 과정에서 실제 구현과 달라졌습니다. 특히 B의 3x·메인 표기는 배속이나 메뉴의 변경 제안이 아닙니다. 비교 범위는 보유 젬 영역의 배치입니다.

## A안 구현

후속 승인으로 A안의 한 줄 인벤토리를 구현했습니다. 기존 금속 소켓 이미지를 사용한 48px 사각 슬롯, 26px 젬 아이콘, 수량 배지, 아래 전체 이름을 유지합니다. 스탯/젬·링크 탭은 28px 높이로 압축하고 전체 너비를 반씩 나눈 연결형 탭으로 구성했고, 링크 홈 제목 옆에 개방 비용을 배치했습니다. `implemented-a-compact-preview.png`는 구현 위젯의 360×800 테스트 렌더이며 일부 기본 글꼴은 테스트 환경에서 사각형으로 표시됩니다.

최종 이름 크기는 8px이며, 목록을 얇은 테두리와 최소 패딩(좌우·위 4px, 아래 2px)으로 묶었습니다. 가로 스크롤 동작은 유지하되 스크롤바는 숨기고 선택한 젬 이름과 효과는 같은 줄에서 자연스럽게 줄바꿈합니다. `implemented-a-live.png`는 실제 웹 게임의 360×800 화면으로, 여섯 번째 젬 일부가 보이는 상태입니다.
