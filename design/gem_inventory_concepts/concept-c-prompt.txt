Use case: ui-mockup. Create one polished, game-realistic mobile tower-defense HUD screenshot concept, 1024x1536 portrait. Input image is a style/composition reference only: keep navy-black space backdrop, muted green stone tile battlefield, cyan steel UI edges and gold accents. Render ALL Korean words crisply and accurately, never copy the missing-glyph white boxes from reference.
Top 40% battlefield: winding tile path, one selected machinegun turret and range circle; compact top resources/HP 20/20, wave 1/40.
Bottom 60% HUD starts with a speed row ABOVE turret detail: 1x, 2x, 4x left, pause control and 시작 right. No enclosing big frame around speed row. Then header “기관총 포탑 Lv.1/10” with upgrade 42G and sell 45G. Then two tabs “스탯” and “젬·링크” (selected). Then label “링크 홈”, exactly THREE physically connected circular mechanical sockets: first empty dark ring, second and third locked padlocks. Beneath sockets show “홈 2 열기” followed by gold coin icon and “90”.
MAIN CONCEPT: Beneath “보유 젬”, a SPLIT view. LEFT 60% width: exactly 3 columns by 2 rows of same-size SQUARE recessed inventory slots. RIGHT 40% width: a fixed selected-item details pane, vertically aligned with grid and approximately same height. Absolutely no separate full-width detail panel below this split.
Each of six square inventory slots has large colored faceted gem icon, small quantity badge, full Korean item name inside near bottom, wrapped into two lines when needed. Not pills, not tags, not horizontal chips. Selected first slot gold border. First row items: yellow lightning gem “가속” x1; orange gem “중화기 증폭” x1; purple gem “연쇄” x1. Second row: teal gem “사거리” x2; pink gem “급소” x1; pale teal gem “조준경” x1.
Right fixed details pane: large yellow lightning gem icon, title “가속”, full effect “공격 속도 40% 증폭”, orange instruction “링크 홈을 선택하세요”, disabled button “장착”. Allow beautiful generous line wrapping for Korean without truncation. These details occur ONCE only, on the right.
Bottom navigation three buttons “포탑”, “업그레이드”, “젬”.
Preserve exact given numeric values. No invented rarities, prices, additional stats, title banners, concept labels, device frames, extra inventory items or extra detail boxes. Show actual game screen edge-to-edge. Clean readable Korean typography; graphic richness stays close to reference, strong functional layout.

TARGETED CORRECTION of the supplied generated concept: Keep its art, Korean text, gems, six grid cells and overall composition unchanged. Add the clearly visible heading '보유 젬' directly above the inventory grid. Make the right details pane begin at precisely the same vertical position as the inventory grid top, below the sockets and '홈 2 열기 coin 90' line, so the grid on left and details on right share the same top and bottom. Slightly compress battlefield vertically to 40% of image to give HUD more room. Remove the thin enclosing frame around the speed/start row: keep individual speed and start button frames only. Preserve all six item names/counts and the existing full effect and instruction; no lower duplicate detail box.
