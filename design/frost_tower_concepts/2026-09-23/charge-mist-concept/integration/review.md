# 게임 통합 독립 검증

2026-09-23, Astra 독립 검증. **PASS — Godot 실제 앱 적용 범위.** 제품 수정은 하지 않았으며 구현자의 자체 판정 대신 변경 코드·검사 조건·실제 렌더를 승인 시안과 대조했다.

| 항목 | 판정·근거 |
| --- | --- |
| 실제 충전·준비 | PASS. `detail-charging.png`에서 실제 쿨타임 중 하단 핀만 청록색이며 상단은 어둡다. `model-hero.png`/`ready-waiting.png`는 전체 충전·적 없는 대기 상태다. 로컬 높이 마스크라 포탑 배치 위치에 종속되지 않는다. |
| 실제 방출·외형 | PASS. `detail-release.png`와 `app-release-slowed.png`에서 방전한 핀과 높이·가림 관계를 가진 서리 안개가 보인다. 승인된 32메시 안개의 질감·부드러운 외곽을 유지하고 원판/빌보드나 구슬로 대체하지 않았다. 청흑금속·청동·6셔터·렌즈·핀·발의 핵심을 유지한다. 주변 게임 조명 차이는 재설계 사유가 아니다. |
| 비회전·중복 제거 | PASS. `main.gd`의 냉각 본체/건설 미리보기 헤드는 고정이며 공용 총구 flash/smoke/반동 경로를 건너뛴다. 기존 `battlefield_effects.gd`의 frost 캔버스 억제를 확인했고 실제 화면에도 중복 원/섬광이 없다. |
| 피해·감속·반경 | PASS. 전투 코드 변경은 `decorate_frame()`의 선택적 9번째 표시 Dictionary뿐이다. 기존 centeredAreaAttack 판정·전체 적 피해/감속·공격 간격·저장 스키마는 변경하지 않는다. 현재 centeredAreaRadius를 타일 단위로 넘기며 실제 앱에서 발사와 slowInstances 발생을 확인한 스크립트/결과를 검토했다. |
| 시간·생명주기 | PASS. 외부 전투 시계와 증가한 shotSequence만 사용한다. pause 전후 실제 이미지가 같은 방출 상태다. 되감기, 초기 snapshot(과거 shot=40, feedback 0/1 모두), 매각·ID 재사용·epoch 파괴·독립 포탑 충전/안개 재질 격리 검사 조건과 실패 0 로그를 확인했다. 4x는 실제 명령 후 0.05초 입력→전투시계 0.2초의 하네스 근거다. |
| 성능 구조 | PASS. 승인 안개 메시·정적 32-instance MultiMesh·노이즈·충전 재질 공유, 각 포탑별 나이/반경 재질과 instance charge를 구분한다. 입자별 매 프레임 CPU 이동은 없다. 최대 4개 무그림자 광원이며 에너지 0에서는 숨긴다. 이는 구조 검증이며 전체 게임 GPU/FPS 보장이 아니다. |

실제 GUI 조건은 Godot 4.7.2 / Apple M4 / Metal Forward Mobile / `--app` 660×1100. 별도 `RuneNexus-Frost-Charge-Integration` 저장을 사용했다. 최초 통합 당시 제품 소스와 실행 프로젝트의 main/runtime/frost 효과·셰이더·계약 검사 파일 SHA-256 일치를 독립 확인했다. 당시 안개 셰이더는 승인 시안과 동일한 `2138164d1d39c94c82151e8756d1512e229401819f20a07b3b0adfd4758417e3`였다. 이후 불투명도 변경의 최종 해시는 아래 후속 검증을 따른다. 포탑 GLB 보존 해시는 실행 결과와 일치한다.

근거는 [계약 검사](contract.log), [기존 전투 7,346개 검사](combat-regression.log), [실제 앱 결과](report.json), [근접 결과](detail-report.json), [검수 스크립트](verify_ingame.gd)다. 기존 결과를 반복 실행하지 않고 실제 검사 내용과 최종 입력 일치 여부를 확인했다.

검수 과정에서 `app-4x.png`는 1x HUD 강조와 검수 카메라 잘림이 있어 4x 화면 증빙에 부적합하다고 반환했다. 최종 README는 해당 이미지를 대표 근거에서 제외하고 4x 전투 시계 검증으로 구분했다. 제품 결함으로 확인된 사항은 아니며 추가 촬영을 요구하지 않는다.

필수 미해결 결함은 없다. 저장에 이전 랜덤 쿨타임 분모가 없으므로 복원 직후의 표시 충전비는 현재 공격속도 기반 추정이다. 실제 남은 쿨타임과 발사 시점은 유지한다. Android 화면·GPU 시간·실기기 성능은 미검증이며 APK를 만들지 않았다. 이전 시안 성능 측정을 게임 전체 부하 보장으로 사용하지 않는다.

## 후속 — 안개 불투명도 소폭 조정

2026-09-23 사용자 요청에 따라 ALPHA 계수만 0.32→0.36(12.5% 증가)한 최종 상태를 독립 실행·검증했다. **PASS**. 이전 [방출 화면](detail-release-before-opacity.png)을 보존한 뒤 같은 격리 프로젝트·660×1100·게임 조명·기존 근접 하네스를 `--detail-only`로 실행했다. [최신 방출 화면](detail-release.png)에서 뒤쪽 솟는 안개와 하체 주변 안개가 약간 더 선명하다. 내부 투과, 부드러운 외곽, 3D 깊이와 본체 가림 관계를 유지하고 딱딱한 경계나 덩어리가 생기지 않았다.

제품 셰이더와 실행 복사본이 일치하며 승인 시안과의 코드 차이는 ALPHA 계수 한 줄뿐이다. 형태·색·반경·시간·움직임은 그대로다. 실행 결과는 [opacity-check.log](opacity-check.log)와 [detail-report.json](detail-report.json)의 실패 0이며 기존 편집기/실행 세션과 사용자 저장을 보존했다. 좁은 시각 변경이므로 기존 계약·전체 전투 검사나 성능 측정을 반복하지 않았다. Android/APK 검증은 수행하지 않았다.

최종 `godot/effects/frost_mist.gdshader` SHA-256: `c49198d1668a5be9479456e3bb44bb4f40b7484bd16c62a151a1a3b9bfd6557c`. 기존 `gameplay.mp4`는 조정 전 자료이며 이 후속 변경의 외형은 최신 근접 PNG가 근거다. 필수 미해결 사항은 없다.
