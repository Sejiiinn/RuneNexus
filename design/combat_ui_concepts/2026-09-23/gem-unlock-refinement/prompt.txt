Use case: ui-mockup.
Create one polished raster visual comparison board of THREE refined alternatives for a mobile fantasy tower-defense turret GEM EQUIPMENT panel. Three equally sized side-by-side LARGE DETAIL CROPS, not complete phones. Each crop includes a small strip of the existing battlefield at top, original selected turret action bar and tabs, the gem equipment interaction, the inventory strip, and bottom navigation. Approx1600x850 board with legible UI. Headings only "1 확장 캡슐", "2 일체형 명패", "3 청록 해금 탭". Dark neutral board background. Three panels same scale, same fixed height, aligned rows.

Reference images: screenshot1 unlock-440.png is current PROBLEM, screenshot2 initial-440.png shows baseline. Preserve battlefield style, actual golden flame shard gem silhouette, bronze engraved round sockets, dark blue-teal panel, original upgrade/traits/sell controls, inventory gem shapes. Redesign ONLY the awkward isolated large double-framed unlock button. Do not copy that separate hanging framed box.

All three panels common: selected cannon row with tiny cannon icon and Korean "기관총 Lv.7", teal "강화 204 G", purple "특성", small gold sell icon; beneath it connected tabs "스탯", selected "젬 링크". EXACTLY THREE connected ROUND equipment sockets centered horizontally. Left socket contains original elongated gold flame shard. Center socket is selected locked socket with warm golden ring and visible lock. Right socket is unselected bronze locked socket. No other round sockets. Each locked socket previews its own price below: selected center "90 G", unselected right "180 G". Price must appear EXACTLY ONCE per locked socket, do not duplicate90G. The selected center alone has an explicit button with exact Korean text "해금". No plus sign, no count changes, no balance changes, no completion message, no cancel, no whole-screen modal. Lock tap selects, explicit unlock button purchases. Each alternative must show an organic compact object physically associated with selected round socket, NOT a disconnected boxed button.

Bottom inventory: ONE HORIZONTAL STRIP of small SQUARE metallic slots running ALL THE WAY to panel right edge. Eight cells total if needed to fill width, first SIX occupied: gold flame shard x2, blue icy spear x2, grey rough mineral x1, turquoise teardrop x2, red pronged crystal x1, silver spike x1. Then TWO EMPTY SQUARE inventory cells with matching border and dark recess, continuing to RIGHT EDGE. They must remain exact small squares equal size, never blank unframed leftover area. These are inventory squares, NOT extra equipment sockets. Small counts bottom right of occupied cells. Same strip in all alternatives. Bottom navigation "포탑", "업그레이드", "젬". No scrollbar. Keep same fixed panel height regardless of interaction. No extra information removed to make room.

Alternative1 EXPANDING CAPSULE: The selected socket's price preview badge expands sideways into a single slim joined capsule directly touching the bottom of the round socket. One unified slim beveled dark-metal outline with cut/chamfered corners, left half coin "90 G", right half modest luminous teal face "해금". A fine subtle seam divides the two halves. It looks like one physical control with the socket, not a popup. No downward connector gap. The unselected third socket retains just a tiny dark gold-edged "180 G" price badge. Capsule around120px wide and32px tall logical scale, visual thickness low. Minimum44px click area can be inferred from surrounding space.

Alternative2 INTEGRATED NAMEPLATE: Center gold round socket and price/action form one coherent metallic vertical fitting: a small narrow metal plate grows directly from the socket bottom contour with shoulders naturally flush to the socket. Plate upper line coin "90 G", lower line "해금", lower face restrained teal. One continuous thin bronze outline around socket and plate, NO nested box border around unlock text. No chain, no gap, no dangling card. Plate smaller than or about same width as socket, around60px wide and44px tall. Right socket has tiny "180 G" price badge. A compact crafted nameplate/lock latch rather than rectangle UI button.

Alternative3 LIGHT TEAL TAB: Center socket keeps its little "90 G" dark metal price badge snug at bottom. Immediately attached under badge a slim shallow teal chamfered tab labeled "해금", almost no frame, soft restrained teal inner light. Continuous silhouette from socket through price tag to tab, no gap and no heavy beveled outer box. Tab about68px wide and26px tall. The price badge and tab are vertically connected but visibly lighter than the solid nameplate of alternative2. Right socket has "180 G" badge only.

All three treatments: match real screenshot dark metal StyleBoxTexture aesthetic, feasible stretchable slim UI trim; no filigree explosion, no decorative chains, no huge frames, no redundant gold coin art. Gold socket material preserved, gem sprites preserved. Short empty detail space above inventory stays reserved. Do not show whole phones or top resource HUD, only enlarged lower UI with battlefield snippet. Distinct alternatives clearly visible, professional production UI mockup, sharp Korean typography.
