Edit this comparison board with ONLY these corrections; preserve three numbered titles, all battlefield/HUD/action bars, exact three equipment sockets per phone, material styling, prices, buttons and panel heights.
1. EACH PHONE bottom INVENTORY must have exactly SIX square48x48 cells, not seven! Left to right: gold flame x2, blue spear x2, grey rock x1, turquoise tear x2, red cluster x1, silver spear x1. Count SIX. Delete duplicated seventh cell in EACH of three phones. Do not fill deleted space with another cell. Keep modest natural blank space to right of this left aligned six-cell row, about65px. Six inventory cells remain equal small squares, not stretched to fill width, no horizontal scrolling.
2. FIRST phone only: REMOVE the long yellow vertical connection line that currently crosses upgrade button and tab. Instead show ONLY a short downward-pointing small gold triangle on the compact popover's bottom edge. No long line anywhere. Selected middle round lock remains gold. Move popover a few pixels up so it doesn't overlap manual dropdown; keep manual dropdown "수동" visible between speed and start, matching other phones. Popover remains above dock, never intersects any UI. Don't alter popover text "90 G" and button "해금".
3. If necessary make unlock button44px minimum touch height by allowing transparent hit padding, don't inflate panel. Second full-width details strip and third compact latch remain exactly as is. All three phones have equal FIXED height panel and inventory row.
Mandatory counts: each PHONE exactly three connected ROUND equipment sockets (gold gem + gold selected lock + dark lock), and exactly SIX SQUARE inventory cells. Three phones total=9 round sockets and18 square inventory cells. Preserve all surrounding artwork.
