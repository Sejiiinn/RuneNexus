Use case: precise-object-edit, ui-mockup.
Edit the supplied RuneNexus full portrait game UI concept. Make ONE very small local change only: in the utility row immediately below the 스탯 / 젬 링크 tabs, replace the plain text "물리 · 경량화기" on the LEFT with TWO separate compact enclosed tag badges, reading exactly "물리" and "경량화기".
Each tag must have its own subtle dark blue-teal background, thin muted steel-blue border, slightly rounded small corners, modest horizontal padding and compact height. Put the two tags beside one another with a small gap. Remove the middle dot. The text is small, pale blue and legible. They are passive category labels, visually quieter and smaller than the neighboring 공격 목표 dropdown button. Not large buttons, no icons, no glow, no ornate frame. Match the existing restrained navy/teal metal game theme.
Preserve absolutely everything else: the "공격 목표  선두 ▼" control on the right, the 7 stats in their same two-column grid with generous spacing and exact numbers, the separate 누적 피해 0.0 bottom strip, all tabs, the entire 기관총 Lv.7 / 강화 204 G / 특성 0/2 선택 / 판매 action line, battlefield, top HUD, bottom navigation, portrait dimensions, positions, fonts, colors and borders. Do not redesign the panel or move any other element. Output one full portrait screenshot concept, with only the two small tags changed.
