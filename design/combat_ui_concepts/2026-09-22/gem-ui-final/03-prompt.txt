Edit this existing Korean RuneNexus UI proposal with precisely the user-requested adjustments. Preserve the entire two-screen board, all battlefield/top HUD/buttons/tabs/fonts/colors and all unrelated layout. RIGHT SCREEN: completely REMOVE the separate '슬롯 추가 · 90 G' rectangular button. In that same horizontal socket band directly below '스탯 | 젬 링크', show EXACTLY SIX circular metallic gem sockets, equal diameter about48 logical pixels, arranged in ONE SINGLE STRAIGHT HORIZONTAL ROW across available width. Connect every adjacent socket with a narrow visible metal link at centerline: six circles, five links, all centers on same Y. No wrapping, no two rows. First socket contains existing gold acceleration gem. Second socket is open empty with plus and selected gold rim. Third, fourth, fifth, sixth sockets are visibly locked dark sockets, each with a small clear padlock inside, their metal rings still readable; third locked socket slightly brighter to communicate next unlock, later ones darker. SIX SLOTS TOTAL: one gold equipped + one selected empty + FOUR locked. No extra separate unlock/add button anywhere. Existing selected blue range gem detail row and 장착 button below remain; lower horizontal gem inventory remains. Make the six sockets fit comfortably with margins rather than cropping. LEFT SCREEN: only reduce the artwork size inside ALL SIX inventory cells by approximately15%, retaining original gemstones' shapes/colors and same centers. Keep cell size, count labels, name and effect type sizes unchanged, giving gemstones a little more breathing space. Do not shrink text. All other UI unchanged. Bottom tabs: LEFT 젬 selected, RIGHT 포탑 selected. Perfect legible Korean, same final board title. The new explicit six-inline-locked-slot design overrides the old two-socket/unlock-button design.
