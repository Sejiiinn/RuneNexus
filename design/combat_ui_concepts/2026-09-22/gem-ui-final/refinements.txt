Edit this RuneNexus UI final proposal board with ONLY these corrections, preserving the same two contexts, gem mappings, Korean labels, metal theme, buttons and composition: make both game screens proper tall 440:900 portrait proportions, extend battlefield area vertically so the bottom inventory/link content occupies only the bottom 28 percent of screen height plus the 40px bottom tabs, and action bar 36px above. The current bottom panels are too large at roughly45%; shrink their contents proportionately and give recovered space to the battlefield. Keep gem icons at40px logical and readable Korean 12-13px. Do not remove any of the six inventory cells or selected gem detail. Keep entire board wide with both full taller phones fitting. In right screen empty second socket MUST have distinct thin GOLD selected ring, while inventory selected blue gem has thin cyan outline. Replace purple crystal inside left gem purchase button with SMALL GREEN SHARD matching top resource HUD, because payment is20 shards. All other elements unchanged, no new details, no new labels. Produce crisp high fidelity final Korean game UI proposal.

Correct ONLY the bottom navigation selection of the RIGHT phone in this image. The right phone shows an equipped turret's Gem Link subtab, so its bottom primary tab selected must be the FIRST tab '포탑'. Give that first tab the cyan highlighted background and border. The right phone LAST bottom tab '젬' must be plain dark inactive like its middle '업그레이드' tab. The LEFT phone stays with its LAST '젬' tab cyan selected. Do not change anything else in the whole image: preserve all pixels of gems, text, panels, battlefield, spacing, sizes, headers, frames and the right gold selected empty socket. This is a tiny navigation state correction, not a redesign.
