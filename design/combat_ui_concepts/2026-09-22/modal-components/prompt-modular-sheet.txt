Use case: precise-object-edit.
Create a MODULAR, ASSEMBLY-READY GAME UI ART ATLAS from the approved reference. Match the reference's exact visual family: slender satin silver/gunmetal clipped corner caps, narrow cyan edge light, smooth deep navy/teal enamel, no stone grain, no chunky bevel. This request changes the ASSET STRUCTURE, not the approved style.

CRITICAL: Do NOT output intact large panels or complete buttons. Output separated construction pieces, with transparent gutters. This must visibly be a kit of corner pieces and straight strips, not the previous complete-component sheet. Large landscape high resolution raster PNG with genuine transparent alpha outside all sprites. Orthographic front view. No words, labels, numbers, grid lines, checkerboard, scenery, text, sample dialogs or usage examples.

Organize neat aligned groups with generous 40px transparent gaps:
UPPER LEFT: modal frame NINE-SLICE pieces displayed as an exploded 3 by 3 arrangement. Row1 top-left corner cap, straight horizontal top-edge strip, top-right corner cap. Row2 straight vertical left-edge strip, plain opaque uniform deep navy rectangular interior tile, straight vertical right-edge strip. Row3 bottom-left corner cap, straight horizontal bottom-edge strip, bottom-right corner cap. FOUR corners all same size and matching metal geometry; FOUR strips exactly match the thickness, color and cross-section of the corners where they join. Strips have open, straight ends: NO decorative caps or tapering at strip ends. The dark interior is visually uniform, no large central highlight or gradient. Retain thin silver rails and cyan highlight from reference.

UPPER RIGHT: title divider built from THREE separate pieces: tiny cyan left triangular endpoint, thin constant-thickness cyan straight line segment, tiny cyan right triangular endpoint. Beneath these separate small glyph sprites: close X in compact matching frame, a gold check, hollow diamond, cyan home, orange exit. Each glyph isolated.

LOWER AREA: FIVE clean equally sized exploded 3x3 NINE-SLICE button kits arranged side by side across the canvas, in reference color order: cyan primary; silver/navy secondary; orange destructive; gold selected; muted blue-gray disabled. EACH kit has its own FOUR fixed-size clipped corners, FOUR straight repeatable border strips, ONE plain interior tile. All five kits share precisely the same corner geometry and rail thickness; only approved reference colors vary. No text/icons baked into these parts. Corner highlights stay inside fixed corner tiles. Horizontal strips have constant appearance along X; vertical strips constant along Y. All centers constant dark enamel color appropriate to their state. Button nine-slice pieces should also serve selection rows at increased width/height, so do not create giant one-piece selection rows.

BOTTOM: connected tabs as isolated modular trim pieces: left outer cap, straight upper rail, thin cyan selected underline segment, straight lower rail, slim vertical join separator, right outer cap; silver and cyan color scheme, shared line thickness. No complete connected tab bar.

High quality crisp extraction-ready sprites. Fixed corners must NEVER depend on stretching. Seamless flat ends on border pieces must permit repetition or single-axis scaling without changing thickness. No exterior haze, background rectangles, drop shadows, glows crossing gutters. Fully opaque dark fills inside sprites and actual alpha outside. Respect consistent pixel scale throughout. The most important visible feature is that all frame/button groups are EXPLODED separate 9-piece sets, with no whole rectangular button or whole frame.
