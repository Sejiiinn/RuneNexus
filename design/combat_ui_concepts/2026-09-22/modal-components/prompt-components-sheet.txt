Use case: precise-object-edit. Input image is the APPROVED visual design master, not loose inspiration. Convert this exact design into an EXTRACTION-READY GAME UI SPRITE SHEET, preserving the precise clipped-corner silhouettes, thin silver/gunmetal rails, corner-cap construction, fine cyan highlights, original navy/teal fills and gold/orange state colors. DO NOT REDESIGN. No increased bevel, no different ornament, no rounded corners, no stone grain. Use a large high-resolution landscape canvas with TRUE TRANSPARENT ALPHA background outside each component. Absolutely NO text, letters, numbers, headings, annotations, sample content, scenery, screenshots, phone frame, checkerboard pattern, layout guide lines or watermark. Components must be isolated and axis-aligned at consistent scale, separated by at least 50 pixels of fully transparent gutters. Nothing touches or overlaps anything else. No shadows or glow leaking into gutters.
Arrange clean separated horizontal rows:
Top row: ONE large EMPTY modal frame matching the reference top-left frame exactly, smooth dark filled interior, same header divider and tiny cyan triangular endpoints, thin silver borders and metallic corner caps. Remove all text, remove embedded close button (provide separately). Next to it ONE smaller empty plain panel with same silver corner caps and interior, without header separator.
Second row: FIVE blank button backgrounds of IDENTICAL dimensions and matching corner geometry: cyan primary, silver/navy secondary, orange/red destructive, gold selected, muted blue-gray disabled. Match the five reference buttons. No icons or words baked in.
Third row: TWO wide blank option-row backgrounds, gold selected and silver/cyan normal, precisely matching the reference selection row outlines. Remove all arrows, check marks, diamond markers and text from inside, leaving continuous clean material fill.
Fourth row: TWO separate blank connected-tab halves: selected cyan LEFT segment and dark unselected RIGHT segment, preserving their outer metal corner caps and straight join edges; ONE separate wide cyan primary button background matching the reference bottom-right confirm button.
Bottom row: separate small close X button matching the reference (thin squared clipped border, slender X), gold check indicator, hollow diamond indicator, gold triple upward arrow glyph, cyan rightward arrow glyph, home glyph, exit glyph. Each one isolated, no enclosing shared panel.
Produce a production-use sprite sheet, not a presentation board. Internal panel/button fills are opaque dark teal, external area actual transparent alpha. Keep matching components consistent and clean for later rectangular crop and 9-slice scaling. Preserve reference design rather than inventing alternatives.
