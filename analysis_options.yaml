analyzer:
  exclude:
    # Historical Flame CPU measurement sources, retained with their old reports.
    - design/stage1_3d/presentation_migration/*_lifecycle/measure_work.dart
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true
